## Structure

generate.sh - bash file for generation

generate_multiple.sh - bash file to use few gpu ids

setup.sh - bash file for setting up repo (git clone + venv + requirements fix)

run_wan.py - main generating file, you can adjust num_inference_steps if needed


